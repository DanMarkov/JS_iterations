for (let counter = 1; counter <= 10; counter++) {
  console.log(counter);
}

// Цикл for имеет три оператора:

// let counter = 1, где вы определяете переменную со значением по умолчанию
// counter <= 10 является условием. После каждой итерации цикл будет проверять, удовлетворяется ли условие, прежде чем выполнить следующую итерацию.
// counter++ является шорткатом. Этот оператор будет выполняться после каждой итерации.

result *= counter;    // то же, что result = result * counter
result += counter;    // то же, что result = result + counter
result -= counter;    // то же, что result = result - counter
result %= counter;    // то же, что result = result % counter

// Добавление единицы к переменной - тоже очень типичная операция, поэтому вместо `counter = counter + 1` можно записать `counter++`. 

// Так же `counter = counter - 1` равносильно `counter--`. Это операторы инкрементирования и декрементирования.

